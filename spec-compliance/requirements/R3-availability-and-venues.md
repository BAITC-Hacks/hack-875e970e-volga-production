# R3 — занятость и площадки

Вердикт: `implemented`.

Все профили проходят единый фильтр города, категории и календаря в `lib/filter.ts:6-18`; занятая дата исключает профиль. Для площадок нет обходного пути. Multi-category площадка проверяется в `tests/core.test.ts:39-42`; исключение занятых — в `tests/core.test.ts:17-22,51-60`.
