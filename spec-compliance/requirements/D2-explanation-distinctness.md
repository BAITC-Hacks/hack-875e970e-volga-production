# D2 — невзаимозаменяемость объяснений

Вердикт: `partial`. Серьёзность: Medium.

Текущий dense smoke-пример содержит три разные особенности. Prompt требует различия, а источник цитаты проверяется. Но код не валидирует смысловую конкретность и отличие, а fallback выбирает первый фрагмент. Для `Лайв-бэнд` профили `HK-23752` и `HK-83709` в fallback получают идентичные итоговые объяснения. См. F-01 в основном отчёте.
