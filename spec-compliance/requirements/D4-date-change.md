# D4 — разные даты

Вердикт: `implemented` для демонстрационной пары 15/16 октября.

Дата влияет на состав через `busy_dates` в `lib/filter.ts:6-18`, но не входит в semantic scoring text в `lib/ai.ts:32-46`; следовательно, изменение множества кандидатов вызвано календарём. `tests/core.test.ts:17-22` подтверждает 6 против 4 кандидатов; `tests/browser/finder.spec.ts:19-29` подтверждает разные top-3. Интерфейс показывает дату, свободу выбранного профиля и число занятых: `app/finder.tsx:71,74,81`.
