# D6 — объяснение пустого результата

Вердикт: `implemented`.

Сервер формирует отдельные словесные сообщения для отсутствующей категории и кандидатов, не прошедших ограничения: `lib/recommend.ts:15-21`. UI показывает отдельный заголовок, сообщение, счётчики причин и дальнейшее действие: `app/finder.tsx:71,81`. Пустой результат имеет HTTP 200, а не ошибку.
