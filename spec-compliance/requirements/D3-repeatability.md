# D3 — повторный запуск

Вердикт: `implemented`.

Проверки детерминированного fallback, tie-break, постоянного кеша, повторных API-запросов и 24 сравнений после перезапуска находятся в `tests/core.test.ts:62-71`, `tests/cache.test.ts`, `scripts/smoke.ts:20-21`, `scripts/verify-runtime.ts:6-10` и `docs/evidence/runtime.json`.
