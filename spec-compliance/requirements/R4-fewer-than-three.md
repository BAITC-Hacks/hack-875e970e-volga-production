# R4 — меньше трёх результатов

Вердикт: `implemented`.

`lib/recommend.ts:35,58-60` возвращает всех допустимых кандидатов, если их один или два, и сообщает отношение eligible/base. `app/finder.tsx:81` показывает количество и пересекающиеся причины исключения. Редкий сценарий с двумя флористами покрыт `tests/core.test.ts:35-38` и `tests/browser/finder.spec.ts:30-33`.
