# D1 — разумное время ответа

Вердикт: `implemented`.

Один deadline 8 секунд охватывает оба внешних этапа в `lib/recommend.ts:23-44`; SDK отключает retries в `lib/ai.ts:7-10`. Сохранённые замеры в `docs/evidence/smoke.json` составляют 7–3405 мс. Живые browser-сценарии прошли на production-контейнере; новый платный прогон на пустом кеше не выполнялся.
