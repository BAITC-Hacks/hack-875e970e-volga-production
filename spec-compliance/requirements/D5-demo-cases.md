# D5 — обязательные демо-запросы

Вердикт: `implemented`.

Пресеты плотной категории, редкого флориста и пустого результата находятся в `app/finder.tsx:5-10`. Они только заполняют форму, после чего пользователь выполняет реальный запрос: `app/finder.tsx:86`. Все сценарии проверены в `tests/browser/finder.spec.ts:19-39` и сохранены в `docs/evidence/smoke.json`.
